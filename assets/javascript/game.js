// Create an array that lists out all options of the alphabet

// Create an array of possible words to guess - is there something for a random selector for the computer to pick a word?

// Create variables for win

//Create variables for guess left

// Create variables for letters guessed so far

//if statements for if user guessses word to randomly select another